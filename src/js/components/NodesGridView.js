/** @jsx React.DOM */

var React = require("react/addons");

var NodesGridView = React.createClass({
  render: function () {
    return (
      <div />
    );
  }
});

module.exports = NodesGridView;
